
@using SES.Models;
@{
    ViewBag.Title = "Biểu phí vận chuyển";
}

@{
    var dict = Model != null ? (Dictionary<string, object>)Model : new Dictionary<string, object>();
    var asset = dict.ContainsKey("asset") ? (Dictionary<string, bool>)dict["asset"] : new Dictionary<string, bool>();
    var listActiveStatus = dict.ContainsKey("activestatus") ? (List<ActiveStatus>)dict["activestatus"] : new List<ActiveStatus>();

}
<script src="@Url.Content("~/Scripts/app/AdminMasterTerritory.js")"></script>
<ul id="tabquanlyvung" class="nav nav-tabs bordered padding-12 tab-color-blue background-blue">
    <li class="active">
        <a href="#s1" data-toggle="tab">Gói cước vận chuyển</a>
    </li>
    <li>
        <a href="#s2" data-toggle="tab">Gói cước theo vùng miền</a>
    </li>
</ul>
<div id="tabQlVM" class="tab-content padding-10">
    <div class="tab-pane fade in active" id="s1">
        @Html.Partial("_DeliveryFeePackage")
    </div>
    <div class="tab-pane fade" id="s2">
        @Html.Partial("_DeliveryFeeTerritory")
    </div>
    
   
</div>

